Systemd :
	sudo systemctl daemon-reload
	sudo systemctl status service@1
	sudo systemctl start service@1
	sudo systemctl stop service@1
	journalctl -xeu nginx-instance@1


// things required:
	nginx.config, pid file, service file, hosting files, log place

Note: pid file location, hosting files, log place must be owned by user www-user.
---------------------------------------------------------------------------------
pid : /run/pid/nginx-site1/
log: /var/log/nginx-site1/
site: /var/www/site1/

--------------------------nginx-config:------------------------------------------
------------------------------------------------------------------------

file:  /etc/nginx-site1/nginx.conf

user www-data;
worker_processes auto;
pid /run/nginx-site1/nginx.pid;
error_log /var/log/nginx-site1/error.log;
include /etc/nginx/modules-enabled/*.conf;

events {
        worker_connections 300;
        # multi_accept on;
}

http {

        ##
        # Basic Settings
        ##

        sendfile on;
        tcp_nopush on;
        types_hash_max_size 2048;
        # server_tokens off;

        # server_names_hash_bucket_size 64;
        # server_name_in_redirect off;

        include /etc/nginx/mime.types;
        default_type application/octet-stream;

        ##
        # SSL Settings
        ##

        ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3; # Dropping SSLv3, ref: POODLE
        ssl_prefer_server_ciphers on;

        ##
        # Logging Settings
        ##

        access_log /var/log/nginx-site1/access.log;

        ##
        # Gzip Settings
        ##

        gzip on;

        # gzip_vary on;
        # gzip_proxied any;
        # gzip_comp_level 6;
        # gzip_buffers 16 8k;
        # gzip_http_version 1.1;
        # gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

        ##
        # Virtual Host Configs
        ##

        server {
                listen 6060;
                server_name _;
                root /var/www/site1;
                index index.html;

                location /{
                        try_files $uri $uri/ =404;
                }

        }
}


#mail {
#       # See sample authentication script at:
#       # http://wiki.nginx.org/ImapAuthenticateWithApachePhpScript
#
#       # auth_http localhost/auth.php;
#       # pop3_capabilities "TOP" "USER";
#       # imap_capabilities "IMAP4rev1" "UIDPLUS";
#
#       server {
#               listen     localhost:110;
#               protocol   pop3;
#               proxy      on;
#       }
#
#       server {
#               listen     localhost:143;
#               protocol   imap;
#               proxy      on;
#       }
#}


	------------------------------------
file:  /etc/nginx-site2/nginx.conf
user www-data;
worker_processes auto;
pid /run/nginx-site2/nginx.pid;
error_log /var/log/nginx-site2/error.log;
include /etc/nginx/modules-enabled/*.conf;

events {
        worker_connections 300;
        # multi_accept on;
}

http {

        ##
        # Basic Settings
        ##

        sendfile on;
        tcp_nopush on;
        types_hash_max_size 2048;
        # server_tokens off;

        # server_names_hash_bucket_size 64;
        # server_name_in_redirect off;

        include /etc/nginx/mime.types;
        default_type application/octet-stream;

        ##
        # SSL Settings
        ##

        ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3; # Dropping SSLv3, ref: POODLE
        ssl_prefer_server_ciphers on;

        ##
        # Logging Settings
        ##

        access_log /var/log/nginx-site2/access.log;

        ##
        # Gzip Settings
        ##

        gzip on;

        # gzip_vary on;
        # gzip_proxied any;
        # gzip_comp_level 6;
        # gzip_buffers 16 8k;
        # gzip_http_version 1.1;
        # gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

        ##
        # Virtual Host Configs
        ##

        server {
                listen 9090;
                server_name _;
                root /var/www/site2;
                index index.html;

                location /{
                        try_files $uri $uri/ =404;
                        }
                }
        }


#mail {
#       # See sample authentication script at:
#       # http://wiki.nginx.org/ImapAuthenticateWithApachePhpScript
#
#       # auth_http localhost/auth.php;
#       # pop3_capabilities "TOP" "USER";
#       # imap_capabilities "IMAP4rev1" "UIDPLUS";
#
#       server {
#               listen     localhost:110;
#               protocol   pop3;
#               proxy      on;
#       }
#
#       server {
#               listen     localhost:143;
#               protocol   imap;
#               proxy      on;
#       }
#}


----------------------------------------------------------
SERVICE FILE:
--------------------------------------------------------

file: /etc/systemd/system/nginx-instance@.service


[Unit]
Description =Nginx Instance %i
After =network.target

[Service]
Type =forking

#
ExecStart =/usr/sbin/nginx -c /etc/nginx-site%i/nginx.conf
ExecReload =/usr/sbin/nginx -s reload -c /etc/nginx-site%i/nginx.conf
ExecStop =/usr/sbin/nginx -s quit -c /etc/nginx-site%i/nginx.conf

PIDFile =/run/nginx-site%i/nginx.pid

Restart =always
RestartSec =5

[Install]
WantedBy=multi-user.target



